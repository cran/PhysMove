#' Example output from calcDisp()
#'
#' Example output from the \code{\link{calcDisp}} function. Displacements calculated using
#' the \code{\link{tracks}} dataset and calcDisp() with max_hr=24. Note that the
#' calcDisp() default is normally max_hr=240; however, this creates an unnecessarily
#' large file for an example dataset so max_hr=24 was used instead.
#'
#' @format list
"disp"
